# Git Commit Rule

At the end of addressing every user request:
- Always stage modified project files and create a local git commit.
- The commit message must be in English.
- The commit message must be concise and descriptive (e.g. `feat: ...`, `fix: ...`, `refactor: ...`), avoiding long messages.
